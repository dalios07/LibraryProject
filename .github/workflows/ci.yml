name: My First CI

on:
  push:

jobs:

  test-job:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Vérifier Java
        run: java -version

      - name: Vérifier Maven
        run: mvn -version